os: windows
language: shell
cache:
  directories:
    - mydir
script:
  - env
  - ls -laR ~/.casher
  - cat ~/fetch.log
  - cat ~/fetch.err.log
  - for i in ~/.casher/*.t?z; do tar tvf "$i"; done
  - cat ~/.casher/bin/casher
  - ls -laR ~
  - ls -la / C:/
  - ls -la mydir
  - grep -rne . mydir/ || echo no files found
  - date -R > mydir/out.txt
  - ls -la mydir
notifications:
  email: false
